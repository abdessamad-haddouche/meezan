[pytest]
pythonpath = .
# Restrict collection to tests/ so a stray root-level script (e.g. a
# manual smoke-test file matching pytest's default "*_test.py" glob) can
# never be imported — and its module-level code executed — as a side
# effect of running `pytest` with no path argument.
testpaths = tests